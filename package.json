{
  "name": "root",
  "private": true,
  "workspaces": [
    "src/packages/*",
    "src/{services,projects}/*/{apps,packages}/*"
  ],
  "scripts": {
    "compile": "lerna run compile",
    "compile:watch": "lerna run compile:watch",
    "clean": "lerna run clean",
    "build": "lerna run build",
    "test": "lerna run test",
    "test:watch": "lerna run test:watch",
    "test:coverage": "lerna run test:coverage",
    "root:test": "jest",
    "root:test:coverage": "jest --collectCoverage"
  },
  "devDependencies": {
    "@types/jest": "^29.4.0",
    "@types/node": "^18.15.0",
    "jest": "^29.5.0",
    "lerna": "^6.4.1",
    "ts-jest": "^29.0.5"
  }
}
