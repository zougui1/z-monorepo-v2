export enum FileType {
  Image = 'image',
  Text = 'text',
  Audio = 'audio',
  Video = 'video',
  Flash = 'flash',
}

export enum ContentType {
  StaticImage = 'staticImage',
  Text = 'text',
  Audio = 'audio',
  Animation = 'animation',
  Flash = 'flash',
}
