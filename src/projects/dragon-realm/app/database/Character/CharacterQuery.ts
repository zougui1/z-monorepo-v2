/*import { CharacterModel, type Character, type CharacterObject } from './CharacterModel';

export class CharacterQuery {
  create = async (character: Character): Promise<CharacterObject> => {
    const document = await CharacterModel.create(character);
    return document.toObject();
  }
}
*/
